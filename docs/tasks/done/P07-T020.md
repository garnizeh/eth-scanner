# P07-T020: Configure Kconfig.projbuild for Menuconfig integration — Completed

**Phase:** P07 - ESP32 Worker - Core Infrastructure  
**Status:** Completed  
**Priority:** High  
**Dependencies:** P07-T010  
**Estimated Effort:** Small

---

## Summary

Added `esp32/Kconfig.projbuild` to expose build-time configuration options via ESP-IDF `menuconfig`. The following config entries are present: `ETHSCANNER_WIFI_SSID`, `ETHSCANNER_WIFI_PASSWORD`, `ETHSCANNER_API_URL`, and `ETHSCANNER_WORKER_ID`.

## Changes made

- Added file `esp32/Kconfig.projbuild` with `EthScanner Configuration` menu and options.

## Verification

- Running `pio run -t menuconfig` will expose the new menu and options.
- Generated `sdkconfig` after configuration will contain `CONFIG_ETHSCANNER_*` entries.

## Notes

- These options can be referenced in C code via `CONFIG_ETHSCANNER_WIFI_SSID`, etc.
- Next task: `P07-T030` (WiFi handler implementation).
